# Core AWS
boto3>=1.34.0
botocore>=1.34.0

# RAG & LLM orchestration
langchain>=0.2.0
langchain-aws>=0.1.0
langchain-community>=0.2.0

# Vector store client
opensearch-py>=2.4.0

# NER & document processing
spacy>=3.7.0

# Relationship graph
networkx>=3.2.0

# PDF processing (for documents not covered by external corpus)
pymupdf>=1.24.0          # fitz — structural PDF analysis including object tree inspection
pdfplumber>=0.11.0       # text extraction with layout preservation

# Data handling
pydantic>=2.0.0          # schema validation for metadata models
python-dotenv>=1.0.0
requests>=2.31.0

# GitHub API (corpus discovery)
PyGithub>=2.1.0

# Utilities
tqdm>=4.66.0             # progress bars for batch ingestion
rich>=13.7.0             # readable CLI output
